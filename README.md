# GitHub Pages Link Shortener

- `index.html` handles redirects.
- `links.json` stores shortlinks.
- `admin.html` adds new shortlinks (commits them to repo via GitHub API).

⚠️ Important: Do not expose a real Personal Access Token in a public repo.
